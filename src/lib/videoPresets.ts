/**
 * video-api ignores any preset choice and always applies its one hardcoded
 * grade (see PHASE1_PRESET_ID in ../../video-api/src/ffmpeg.ts). Keep this
 * name in sync with that until Phase 3 adds real preset selection.
 */
export const PHASE1_PRESET_NAME = 'The Dark Knight';
